import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/ThemeProvider";
import { AuthProvider } from '@/components/AuthProvider';
import ChatbotWidget from '@/components/ChatbotWidget';

const geist = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NewsMonkey - Your Trusted News Source",
  description: "Get the latest breaking news, live updates, and comprehensive coverage from around the world.",
  keywords: "news, breaking news, live updates, current events, journalism",
  authors: [{ name: "NewsMonkey Team" }],
  viewport: "width=device-width, initial-scale=1",
  robots: "index, follow",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geist.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>
          <ThemeProvider
            attribute="class"
            defaultTheme="system"
            enableSystem
            disableTransitionOnChange
          >
            {children}
            <ChatbotWidget />
          </ThemeProvider>
        </AuthProvider>
      </body>
    </html>
  );
}
